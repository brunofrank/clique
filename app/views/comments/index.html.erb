<div class="w-full">
  <div id="comments" class="min-w-full">
    <%= turbo_frame_tag "post_#{@post.id}_comments" do %>
      <%= render CommentItemComponent.with_collection(@comments, current_user: current_user) %>
    <% end %>
  </div>
</div>
